# Design: Agent Butterfly Lab

Generated by /office-hours on 2026-09-04
Branch: main
Repo: tiller
Status: APPROVED
Mode: Builder

## Problem Statement

Agent trajectories are non-deterministic and nobody can say *why* one run
diverged from another. The whole observability category traces what happened;
none of it can answer the counterfactual, "what would this agent have done if
one thing had been different."

tiller is positioned to answer that, by accident. Because the driver emits a
quoted MFA term and every `{action, result}` pair lands in an ordered log,
tiller is event-sourced rather than checkpointed. The state at turn N is
`Enum.take(log, n)`. There is no checkpointer to configure and no per-step
snapshot cost, so every turn is a free fork point.

Build the instrument that exploits that: fork any turn into N branches, each
mutating exactly one variable, race them concurrently under the supervisor,
and report the first divergent event plus the smallest mutation that changed
the outcome.

Scope note: this is a learning project, chosen for fun. Adoption is not a goal.

## What Makes This Cool

- **Fork is free.** LangGraph Studio requires a compiled checkpointer or time
  travel does not work at all. tiller needs none, because the log prefix *is*
  the state.
- **Replay is deterministic.** LangGraph re-executes nodes on replay, LLM calls
  included, so results can differ. Replaying a recorded prefix with injected
  results does not.
- **Nobody races branches.** Every tool surveyed explores forks one at a time.
  N concurrent branches is `DynamicSupervisor.start_child` called N times, and
  a threading problem everywhere else.
- **Capability counterfactuals ship nowhere.** "What if the agent had not been
  allowed to call this tool at turn 3" appears in arxiv papers and in no
  product. In tiller the whitelist is already a per-session parameter
  (`lib/tiller/session.ex:22`), so the mutation axis costs a keyword.
- **Process kill as a mutation axis.** Only casual on the BEAM, though see
  Open Question 5 for why it is harder than it first looks.

## Constraints

- Weekend-scale project, though the chosen approach is honestly multi-weekend.
  No upper bound has been set; the go/no-go in The Assignment is the only gate.
- Elixir 1.20, currently zero dependencies. Phoenix/LiveView is an accepted,
  permanent change to that character (premise 5).
- No real LLM driver. FakeDriver is the contract and stays the contract.
- `Tiller.Tools` currently holds `echo/1` and `fail/0`. The counterfactual is
  meaningless until there are three or four genuinely different tools.
- **The existing test suite does not survive this.** 4 of the 5 tests in
  `test/tiller_test.exs` assert `{:halted, n} = Session.run(pid)` synchronously
  and pattern-match `State.log()` as bare `{action, result}` 2-tuples. Both
  contracts change. Only `"bad action term is rejected"` survives, because it
  calls `Actions.eval/2` and touches neither module. `Tiller.Demo` breaks for
  the same reason.

## Premises

1. tiller is event-sourced, not checkpointed. Forking needs no checkpointer and
   costs nothing per turn, because the prefix of the log IS the state.
2. **(REVISED)** Making sessions async and attributed is the main event, not
   setup. Originally estimated at ~30 lines; Codex read the source and put the
   runtime core at 90-140 lines and the Session/State inversion slice at
   250-400. Revision accepted. The specific miss: `spawn_subagent` calls
   `Tiller.Session.run/1` synchronously at `lib/tiller/actions.ex:59`, from
   inside the parent's own `handle_call(:run, ...)`, so the parent blocks on
   the child's entire run. Inverting the session loop alone does not fix this.
3. Concurrent N-way fork racing is the differentiator. Cheap on the BEAM,
   expensive in every tool surveyed.
4. Per-fork whitelist variation is a counterfactual nobody ships and is nearly
   free here.
5. Crowded space is not a disqualifier for a learning project. The deliverable
   is a striking demo and real understanding.

## Cross-Model Perspective

Codex (codex-cli 0.153.2, read-only against the repo, no session context):

- **Coolest unconsidered version:** "agent butterfly-effect lab." Click any
  turn, launch branches that each mutate one variable (tool result, whitelist,
  artificial latency, process kill/restart, driver choice). Race 32 branches,
  automatically identify the first divergent event and the smallest mutation
  that changed the outcome. The UI becomes an agent autopsy tool, not merely a
  replay viewer. *This became the chosen approach.*
- **Key insight:** the strongest signal was wanting to "watch [runs]
  concurrently under supervisor finish different times." The excitement is
  visible BEAM behavior: process isolation, timing, failure, causality.
  LiveView is valuable because it exposes the spectacle. The LLM is secondary.
- **Existing tools:** LangGraph gets ~50% of the concept (stateful runtime,
  checkpoint history, replay/fork semantics). Still to build: BEAM session
  processes with DynamicSupervisor integration, attributed trajectories rather
  than graph checkpoints, live event streaming into Phoenix, N-way race,
  whitelist counterfactuals, fault injection, first-divergence analysis.
- **Challenged premise:** CLAIM D's 30-line estimate, wrong by 3-5x. Cited
  `session.ex:43` (whole loop inside `handle_call/3`), `actions.ex:59` (blocking
  `spawn_subagent`), `state.ex:23` (global list accepting only
  `{action, result}`). "Attribution requires identity, branch ancestry,
  ordering, subscription, fork-safe replay semantics, not just adding an
  argument." Accepted.
- **Also caught:** `Tiller.Driver.Replay` must cache replayed results so forked
  prefixes do not execute side effects twice. Replaying *actions* re-runs the
  tool; deterministic replay requires injecting the recorded *result*.

Where Claude disagreed: LangGraph gets 50% of the concept and 0% of the code,
since none of it is Elixir and the checkpoint model is the thing being
deliberately avoided.

An independent reviewer subagent scored the first draft of this document 6/10
and found 13 issues, all incorporated here. It independently verified three
code claims as TRUE: the whitelist is a per-session parameter at
`session.ex:22`, `spawn_subagent` blocks, and `State.append` is O(n) at
`state.ex:33`. It corrected the blocking-call citation from `actions.ex:52`
(the `def` line) to `actions.ex:59` (the call site), and caught the test-suite
break, the process-kill restart mismatch, and the effort-anchor scope error.

## Approaches Considered

- **A: Vertical Slice.** Codex's minimal plan, branches vary by driver.
  Rejected: branches differing only by which script you handed them shows
  concurrency, not insight.
- **B: Event Core First.** Foundation with tests before any UI. Rejected:
  nothing on screen until late, the standard way a fun project dies.
- **C: Whitelist Butterfly.** One mutation axis (capability), same cost as A.
  Rejected in favor of D, but remains the documented fallback if D proves
  intractable.
- **D: Full Butterfly Lab.** Chosen. See below.

## Recommended Approach

**D: Full Butterfly Lab.** Effort XL, risk High.

A first-class `Tiller.Mutation` vocabulary applied per-branch, raced
concurrently, with automatic divergence analysis.

### Mutation axes

| Axis | Mechanism | Cost | Tier |
|---|---|---|---|
| Whitelist restriction | `whitelist:` keyword to `Session.child_spec` | already plumbed | MVP |
| Driver swap | `driver:` keyword | already plumbed | MVP |
| Result override | Replay driver injects a different recorded result | small | Stretch |
| Injected latency | `Process.sleep` in the turn scheduler | small | Stretch |
| Process kill / restart | see Open Question 5 | **High, not medium** | Stretch |

### Types

These need to exist before anyone writes `Tiller.State` or `Tiller.Mutation`.

```elixir
@type action :: {:call, module, atom, [term]}

defmodule Tiller.Event do
  defstruct [:seq, :session_id, :parent_id, :turn, :action, :result]
end

@type event :: %Tiller.Event{
        seq: pos_integer,             # monotonic, globally ordered
        session_id: binary,           # this branch
        parent_id: binary | nil,      # branch ancestry; nil for root
        turn: non_neg_integer,        # turn index within this session
        action: action | :halt,
        result: {:ok, term} | {:error, term} | {:halted, non_neg_integer}
      }

# Halt records: action is the atom :halt, result is {:halted, turn_count}.
# This replaces the current s.state.append(:halt, s.turns) at session.ex:51.

@type mutation ::
        {:whitelist, [{atom, arity}]}
      | {:driver, module, ctx :: term}
      | {:result_override, turn :: non_neg_integer, term}
      | {:latency, milliseconds :: pos_integer}
      | {:kill_at, turn :: non_neg_integer}
```

### Foundation API

```elixir
Tiller.Session.run(pid)                              :: :ok
Tiller.Session.await(pid, timeout \\ 5_000)          :: {:halted, non_neg_integer} | {:error, :timeout}
Tiller.Session.fork(pid, turn, mutation, opts \\ []) :: {:ok, pid}
Tiller.State.append(session_id, parent_id, turn, action, result) :: {:ok, event}
Tiller.State.events(session_id)                      :: [event]
Tiller.State.subscribe(session_id)                   :: :ok
```

`await/2` exists specifically so the test suite and `Tiller.Demo` can still
assert deterministically once the loop is async. It is built on
`State.subscribe/1`, not on a sleep.

### Foundation work

- `Session.run/1` becomes a `cast`, one turn per scheduled message, so the
  process is observable mid-run.
- `spawn_subagent` stops calling `Session.run/1` synchronously
  (`lib/tiller/actions.ex:59`).
- Events carry `session_id`, `parent_id`, `turn`, and a monotonic sequence.
- `Tiller.State` becomes an ordered event store with per-session filtering,
  replacing the single global list. Fix `log ++ [event]` (O(n) per append,
  `state.ex:33`) at the same time.
- `Phoenix.PubSub` broadcast on append.
- A minimal LiveView tail on the event stream, enough to prove events arrive
  live. The 3-pane inspector is Next Step 9, not part of this.

`Tiller.Driver.Replay` is deliberately NOT in the list above; it is tracked as
Next Step 7 and gated on Open Question 2.

**Effort scope, stated precisely:** the 250-400 line figure covers the
Session/State inversion, attribution, PubSub, and a minimal LiveView tail.
It does **not** cover `Tiller.Mutation`, `Tiller.Driver.Replay`, the
divergence algorithm, the 3-pane UI, or fault injection. Approach D as a
whole is not costed and is honestly multi-weekend.

Rationale: it is the only version that turns the screen from a spectacle into
an instrument, and the divergence analysis is the part that makes it worth
building rather than watching. Chosen against the recommendation (C), knowingly.

## Open Questions

1. **Is divergence detection tractable?** Aligning two sequences of quoted
   terms is the one genuinely algorithmic piece and nothing else in the design
   is hard. Unresolved. This is what The Assignment attacks.
2. **How does `Tiller.Driver.Replay` hand off to its delegate?** Driver `ctx`
   is opaque and driver-specific: `FakeDriver`'s is `%{queue: [...]}`, a real
   driver's will hold conversation state. Replay cannot construct a delegate's
   ctx from recorded events. Two candidate answers: require the delegate's
   initial ctx be supplied at fork time, or add an optional
   `Tiller.Driver.resume_ctx(events)` callback. Undecided. Blocks Next Step 7.
3. **What does "smallest mutation" mean when axes are incommensurable?** A
   whitelist removal and 200ms of latency are not on the same scale. Options:
   rank within an axis only, or hand-assign weights. Undecided.
4. **How do 32 branches lay out on one screen?** Flagged as a serious problem
   by both models, solved by neither. Explicitly Stretch, not part of Done.
5. **Process kill is not a resume.** `DynamicSupervisor`'s default
   `:one_for_one` restart re-invokes the child's original `child_spec` start
   args, so a killed branch comes back with its *initial* ctx and `turns: 0`.
   That is a fresh duplicate branch, not a branch resumed from the kill point.
   Resuming requires capturing and re-injecting mid-run state, which is another
   Replay problem. This is why the axis is costed High. Must be resolved before
   Next Step 10.

## Success Criteria

**MVP (this is "Done"):** pick a turn in a recorded run, fan out to at least 4
branches across the two already-plumbed axes (whitelist, driver), watch them
finish at different times in one LiveView, and have `Tiller.Divergence` report
the turn at which they first diverged.

**Stretch (explicitly not required for Done):** the `result_override`,
`latency`, and `kill_at` axes; 32 concurrent branches and the layout problem
that comes with them; ranking by smallest decisive mutation.

Deliberately out of scope entirely: real LLM drivers, persistence across
restarts, auth, generic serialization, production styling.

## Distribution Plan

None required. This runs locally with `mix phx.server`. If it earns being
shown, the artifact is a screen recording of the race, not a release.

## Next Steps

Step 1 is a go/no-go. **Steps 2 through 10 are contingent on it passing.**

1. **Divergence spike** (see The Assignment). Go/no-go on D. If it fails, drop
   to approach C.
2. Give `Tiller.Tools` four real tools with side effects and distinct failure
   modes. Without them nothing downstream is demonstrable.
3. Define `Tiller.Event` and the `mutation` type from the Types section.
4. Invert `Tiller.Session`: `cast`-based loop, one turn per message, add
   `await/2`, and unblock `spawn_subagent` at `lib/tiller/actions.ex:59`.
5. Rebuild `Tiller.State` as an attributed ordered event store with PubSub.
   Kill the `log ++ [event]` append while in there.
6. **Rewrite `test/tiller_test.exs` and `Tiller.Demo`** against `await/2` and
   the new event shape. 4 of 5 tests and the demo break at step 4; this is not
   optional cleanup.
7. `Tiller.Driver.Replay` with recorded-result injection. **Blocked on Open
   Question 2** (delegate ctx hand-off); resolve that first.
8. `Tiller.Mutation` with the two MVP axes (whitelist, driver).
9. One LiveView: timeline left, turn detail middle, live race right. Wire
   divergence analysis into the race view. This is the MVP bar.
10. Stretch axes: `result_override`, `latency`, then `kill_at`. **`kill_at` is
    blocked on Open Question 5** (restart is a duplicate, not a resume).

## The Assignment

Before touching `Session` or `State`, spend 30 minutes writing
`Tiller.Divergence.first_diff/2` over two hand-written lists of quoted terms.

```elixir
@spec first_diff([event], [event]) ::
        :identical
      | {:diverged, index :: non_neg_integer, left :: event | nil, right :: event | nil}
# left or right is nil when one branch ran out of turns first.
```

Three test cases: identical prefixes then a different tool, identical actions
with different results, and one branch halting early. No GenServers, no
Phoenix, no async. Just the function and `mix test`.

This is the only genuinely algorithmic piece in the whole design and everything
else is plumbing you already know how to write. If it is obviously right inside
30 minutes, D is real and you should build it. If you are still fighting
alignment semantics at minute 45, D is a multi-weekend project wearing a
weekend costume, and the honest move is to drop to approach C, the whitelist
butterfly, which needs no divergence analysis to be interesting.

Do the spike first. It is a go/no-go, and it costs half an hour.

## Spike result (2026-09-06): GO

`Tiller.Divergence.first_diff/2` exists (`lib/tiller/divergence.ex`) with
the three assigned cases plus five more in `test/divergence_test.exs`. It
was obviously right well inside the 30 minutes, for a reason the
assignment hinted at: a branch forked at turn N shares its parent's first
N events by construction, so "first divergent event" is a prefix walk, not
a sequence alignment. An inserted turn IS the divergence; there is no edit
distance to compute. Approach D stands.

The one real decision was the equality projection. Raw `==` calls two
branches divergent because a subagent got a different pid or a crash's
stack trace lists a different line, so `normalize/1` erases pids, refs,
ports, funs, and the stack trace inside `{:error, {kind, reason, st}}`,
and `key:` lets a caller project further (a seed envelope's timestamps).
This is the seam open question 3 (incommensurable axes) will eventually
hang from, and it is unchanged by that question.

Also landed, because the Types section says they come first:
`Tiller.Event` (`lib/tiller/event.ex`) with `from_log/2`, so the analysis
is written once against the attributed shape and runs on today's raw
`{action, result}` log too. Nothing writes events yet; steps 4 and 5 do.

## Foundation slice (2026-09-06): steps 2 to 6 done

- **Step 2, real tools:** met by the `seed_*` verbs
  (docs/designs/open-seed-integration.md). Four worker verbs with four
  distinct, engine-decided failure modes: contention (2), invalid
  transition (3), not found (4), fenced out (6). No local stand-ins
  were added; the mock server in `test/support/` scripts those refusals
  for engine-free runs.
- **Step 3, types:** `Tiller.Event` (`lib/tiller/event.ex`) and the
  `Tiller.Mutation.t` type (`lib/tiller/mutation.ex`, type only;
  `plumbed?/1` names the two axes `Session` options already express).
- **Step 4, invert Session:** `run/1` is a cast, one turn per `:turn`
  message; `await/2` is built on `State.subscribe/1`; `run_to_halt/2` is
  the synchronous convenience. `spawn_subagent` no longer calls
  `Session.run/1` synchronously: it returns `{:subagent_started, pid,
  session_id}` and the parent keeps taking turns; the child's events
  carry `parent_id`. Inside a tool the session's id comes from
  `Session.current_id/0` (process dictionary), because a tool runs in
  the session process and cannot `call` it.
- **Step 5, State:** an attributed ordered event store, newest-first
  internally (the O(n) append is gone), `events/1` per session,
  `events/0` in `seq` order, `log/1` as the human `{action, result}`
  projection. **Deviation, declared:** `subscribe/1` dispatches through a
  `Registry` (zero dependencies) rather than `Phoenix.PubSub`. Same
  message shape, same seam; step 9 swaps the transport when Phoenix
  arrives, and nothing above `State.subscribe/1` changes.
- **Step 6, tests and demo:** `test/tiller_test.exs` rewritten against
  `run_to_halt/2`, `await/2`, and `%Tiller.Event{}`; `Tiller.Demo.run/0`
  prints the interleaved event log, where the subagent's first turn
  lands before the parent's own spawn record because the parent no
  longer waits.

## Steps 7 and 8 (2026-09-06): replay, fork, race

**Open question 2, decided:** `Tiller.Driver` gains an optional
`resume_ctx(initial_ctx, replayed_events)` callback. Only a driver can
rebuild its own opaque context, so the driver is asked, with its initial
context and the events the branch re-lived (overrides included: that is
the past this branch experienced). A driver without the callback is
handed its context exactly as supplied at fork time, so the caller
positions it. `FakeDriver.resume_ctx/2` drops the replayed turns from
the script; an LLM driver will rebuild its conversation from the events.

- **Step 7, `Tiller.Driver.Replay`:** answers each prefix turn as
  `{:replay, action, result, ctx}`, a third driver reply shape the
  session records without executing (`origin: :replay` on the event).
  Codex's catch holds: replaying actions would re-run tools, so the
  recorded *result* is injected. The test proves it with
  `spawn_subagent`: the branch's replayed spawn carries the parent's
  child pid and no second child appears under the supervisor. The
  `overrides` map substitutes a result at a replayed turn, which made
  the `result_override` axis (Stretch) fall out of step 7 for free.
- **Step 8, `Tiller.Session.fork/4`** applies one mutation at one turn:
  `whitelist`, `driver`, `result_override`, and `latency` (a delay before
  each live turn; also free). `kill_at` is refused with
  `{:unsupported, :kill_at}` until open question 5 is answered. An
  override outside the prefix and a turn beyond the log are refused too.
  Branches run under `Tiller.Supervisor` with `parent_id` set to the
  forked session, `fork_turn` and `mutation` in `info/1`.
- **`Tiller.Lab.race/4`** is the MVP without the screen: fork N, run all
  before awaiting any, await all, and report `Divergence.first_diff/3`
  per branch. `Tiller.Demo.butterfly/0` prints it. Step 9's LiveView
  calls this and paints `State.subscribe(:all)`.

Success criteria check: the MVP asked for at least 4 branches across
the two plumbed axes, watched finishing at different times, with the
first divergence reported. Everything but "watched" (the LiveView) is
met, and four axes are live rather than two.

Next: step 9, the LiveView (adds Phoenix; swap `State.subscribe/1` to
`Phoenix.PubSub`). Then open question 5 for `kill_at`.

## What I noticed about how you think

- You wrote "Deliberate limits (upgrade paths)" as a first-class README section
  on a two-commit scaffold, and marked `Tiller.State` with "ponytail: in-memory
  list, newest at the tail. Move to ETS/Ecto when you need replay across
  restarts." Most people document what a thing does. You documented where it
  ends and what to do about it.
- "One thing, three jobs" is how you described the action log. That compression
  is the reason the fork idea works at all. You built an event-sourced system
  and described it as a log, which is why the property was sitting there unused.
- You chose LiveView over zero-dependency git files, and then chose D over C,
  both against the recommendation, without arguing about it. Twice in one
  session is a pattern, not a mood.
- When Codex said your estimate was wrong by 3-5x, you took it and revised
  rather than defending. Then immediately picked the most expensive option
  anyway. Those two moves together are unusual: you updated on the cost and
  decided the cost was acceptable, which is different from not believing it.
